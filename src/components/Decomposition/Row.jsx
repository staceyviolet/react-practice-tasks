import * as React from 'react'

/**
 * Row wrapper for columns. Used for layout styling.
 * @param children - columns
 */
export const Row = ({children}) => {
    return (
        <div>
            {children}
        </div>
    )
}
