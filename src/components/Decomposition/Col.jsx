import * as React from "react";

/**
 * Column wrapper for elements. Used for layout styling.
 * @param children - content blocks
 */
export const Col = ({children}) => {
    return (
        <div>
            {children}
        </div>
    )
}
